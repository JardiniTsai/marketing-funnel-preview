# Session: 2026-04-18

## beckv1.html Page2 最終版本記錄

**版本**: `b9e8c72` (2026-04-18 23:46)

**CSS 參數**:

```css
/* 整體布局 */
.page2-new-layout {
  grid-template-columns: 40% 50%;
  gap: 5%;
  margin: 46px 0 50px 150px;   /* 右移 150px */
  transform: scale(0.88);       /* 整體縮小 */
  transform-origin: left center;
}

/* 左側圖片容器 */
.page2-left-image {
  width: 77%;                   /* 縮小後寬度 */
  padding-right: 5px;
  transform: translateX(130px); /* 左圖額外右移 130px */
}

/* 左側圖片 */
.page2-reference-image {
  width: 100%;
  height: 100%;
  object-fit: contain;          /* 完整顯示不裁切 */
}

/* 右側文案容器 */
.page2-content-block {
  width: calc(77% - 70px);      /* 縮小後寬度 -70px */
  max-width: calc(77% - 70px);
  height: 100%;
}

/* 底部問題 */
.page2-bottom-question {
  padding-bottom: 60px;         /* 原始高度 */
  padding-top: 40px;
  margin-top: -5px;
  transform: translateY(-5px);  /* 往上移 5px */
}
```

**關鍵調整歷程**:
1. 左匡與右匡原始寬度：85%
2. 圖片顯示方式：`object-fit: contain`（完整顯示）
3. 整體縮小：81% → 77%, scale 1.03 → 0.88（約縮小 15%）
4. 左圖右移：`transform: translateX(130px)`
5. 右匡寬度減少：`calc(81% - 70px)` → `calc(77% - 70px)`
6. 整體布局右移：`margin-left: 150px`
7. 底部高度保持：60px

**預覽 URL**:
https://jardinitsai.github.io/marketing-funnel-preview/beckv1.html?v=202604182346
